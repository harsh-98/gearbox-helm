apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gb-rpc.fullname" . }}-test-connection"
  labels:
    {{- include "gb-rpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gb-rpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

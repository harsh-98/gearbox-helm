apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redstone.fullname" . }}-test-connection"
  labels:
    {{- include "redstone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redstone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
